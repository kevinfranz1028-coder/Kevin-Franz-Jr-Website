<nav class="navbar">
    <div class="container">
        <div class="navbar-wrapper">
            <a href="/" class="logo">{{ navigation.siteName }}</a>

            <button class="menu-toggle" aria-label="Toggle menu">
                <span></span>
                <span></span>
                <span></span>
            </button>

            <ul class="nav-links">
                {% for link in navigation.links %}
                <li><a href="{{ link.url }}" {% if page.url == link.url %}class="active"{% endif %}>{{ link.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>
