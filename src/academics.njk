---
layout: base.njk
title: {{ pages.academics.title }}
description: Academic profile for Kevin Franz Jr. - GPA, coursework, awards, and resume for Class of 2028 student-athlete.
permalink: /academics/
---

<section class="section-page-header">
    <div class="container">
        <div class="page-header-centered">
            <span class="label-eyebrow">{{ pages.academics.title }}</span>
            <h1>{{ pages.academics.heading }}</h1>
            <p class="hero-subheadline">{{ pages.academics.subheading }}</p>
        </div>
    </div>
</section>

<section class="section-content">
    <div class="container">
        <div class="content-grid">
            <div class="content-main">
                <h2>Academic Overview</h2>
                <div class="info-list">
                    <div class="info-item"><strong>GPA:</strong> {{ pages.academics.overview.gpa }} (Unweighted){% if pages.academics.overview.gpaWeighted %} / {{ pages.academics.overview.gpaWeighted }} (Weighted){% endif %}</div>
                    <div class="info-item"><strong>Class Rank:</strong> {{ pages.academics.overview.rank }}</div>
                    <div class="info-item"><strong>School:</strong> {{ pages.academics.overview.school }}</div>
                    <div class="info-item"><strong>Anticipated Graduation:</strong> {{ pages.academics.overview.graduation }}</div>
                </div>

                <h2>Current & Recent Coursework</h2>
                {% if pages.academics.coursework.honorsAP.length > 0 %}
                <h3>Honors / AP Classes</h3>
                <ul>
                    {% for course in pages.academics.coursework.honorsAP %}
                    <li>{{ course }}</li>
                    {% endfor %}
                </ul>
                {% endif %}

                {% if pages.academics.coursework.stem.length > 0 %}
                <h3>STEM Focus</h3>
                <ul>
                    {% for course in pages.academics.coursework.stem %}
                    <li>{{ course }}</li>
                    {% endfor %}
                </ul>
                {% endif %}

                <h2>Academic Interests & Goals</h2>
                <div>{{ pages.academics.interests | safe }}</div>

                {% if pages.academics.awards.length > 0 %}
                <h2>Awards & Recognition</h2>
                <ul>
                    {% for award in pages.academics.awards %}
                    <li>{{ award }}</li>
                    {% endfor %}
                </ul>
                {% endif %}

                {% if pages.academics.resumePdf %}
                <div class="cta-section">
                    <a href="{{ pages.academics.resumePdf }}" class="btn-primary" download>Download Academic Resume (PDF)</a>
                </div>
                {% endif %}
            </div>

            <div class="content-sidebar">
                {% if pages.academics.image %}
                <img src="{{ pages.academics.image }}" alt="{{ player.name }} academic photo" class="sidebar-image">
                {% endif %}
            </div>
        </div>
    </div>
</section>
